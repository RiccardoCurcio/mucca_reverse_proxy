version: '3'
services:
  nginx-reverse:
    build: .
    image: nginx:reverse
    container_name: {{SERVICENAME}}
    ports:
      {{PORTCONFIG}}
    networks:
      - {{APPLICATIONNETWORK}}

networks:
  {{APPLICATIONNETWORK}}:
    external: true
